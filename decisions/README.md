# Decisions

Agent Zero decision queue and append-only decision events per SDD-3A-MASTER
Appendix C (canonical decision taxonomy). Every decision names its type,
authority, and exact target (digest, immutable ID, or typed durable-artifact
reference). Empty until the master is accepted.
